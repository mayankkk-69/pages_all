/* ============================================
   header.js — Header Section Logic
   ============================================ */

(function () {
    const avatar = document.getElementById('hrHeaderAvatar');
    if (!avatar) return;

    avatar.addEventListener('click', () => {
        // Placeholder for future profile dropdown
        console.log('[header.js] Avatar clicked');
    });
})();
